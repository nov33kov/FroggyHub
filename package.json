{
  "name": "froggyhub",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "http-server -c-1",
    "build": "echo 'No build step'",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0",
    "http-server": "^14.1.1"
  },
  "lint-staged": {
    "*.{js,css,html,md}": [
      "prettier --write",
      "eslint --fix"
    ]
  }
}
